#pragma config(UART_Usage, UART2, uartNotUsed, baudRate4800, IOPins, None, None)
#pragma config(Motor,  port1,           rollerMotor,   tmotorVex393_HBridge, openLoop)
#pragma config(Motor,  port2,           intakeMotor,   tmotorVex393_MC29, openLoop)
#pragma config(Motor,  port3,           leftVerticalMotor, tmotorVex393_MC29, openLoop)
#pragma config(Motor,  port4,           rightVerticalMotor, tmotorVex393_MC29, openLoop)
#pragma config(Motor,  port5,           leftFrontMotor, tmotorVex393_MC29, openLoop)
#pragma config(Motor,  port6,           rightFrontMotor, tmotorVex393_MC29, openLoop)
#pragma config(Motor,  port7,           leftBackMotor, tmotorVex393_MC29, openLoop)
#pragma config(Motor,  port8,           rightBackMotor, tmotorVex393_MC29, openLoop)
#pragma config(Motor,  port9,           puncherMotor,  tmotorVex393_MC29, openLoop)
//*!!Code automatically generated by 'ROBOTC' configuration wizard               !!*//
/*---------------------------------------------------------------------------*/
/*                                                                           */
/*        Description: Competition template for VEX EDR                      */
/*                                                                           */
/*---------------------------------------------------------------------------*/

// This code is for the VEX cortex platform
#pragma platform(VEX2)

// Select Download method as "competition"
#pragma competitionControl(Competition)

//Main competition background code...do not modify!
#include "Vex_Competition_Includes.c"

  int score_mode=0;
  int timeDrop=0;
  void go_auto();
	void move2D(int motorPower, int turnPower);



/*---------------------------------------------------------------------------*/
/*                          Pre-Autonomous Functions                         */
/*                                                                           */
/*  You may want to perform some actions before the competition starts.      */
/*  Do them in the following function.  You must return from this function   */
/*  or the autonomous and usercontrol tasks will not be started.  This       */
/*  function is only called once after the cortex has been powered on and    */
/*  not every time that the robot is disabled.                               */
/*---------------------------------------------------------------------------*/

void pre_auton()
{
  // Set bStopTasksBetweenModes to false if you want to keep user created tasks
  // running between Autonomous and Driver controlled modes. You will need to
  // manage all user created tasks if set to false.
  bStopTasksBetweenModes = true;


	// Set bDisplayCompetitionStatusOnLcd to false if you don't want the LCD
	// used by the competition include file, for example, you might want
	// to display your team name on the LCD in this function.
	// bDisplayCompetitionStatusOnLcd = false;

  // All activities that occur before the competition starts
  // Example: clearing encoders, setting servo positions, ...
}

/*---------------------------------------------------------------------------*/
/*                                                                           */
/*                              Autonomous Task                              */
/*                                                                           */
/*  This task is used to control your robot during the autonomous phase of   */
/*  a VEX Competition.                                                       */
/*                                                                           */
/*  You must modify the code to add your own robot specific commands here.   */
/*---------------------------------------------------------------------------*/

task autonomous()
{
	//score_mode=3;
	go_auto();
}

void go_auto()
{
  // ..........................................................................
  // Insert user code here.
  // ..........................................................................

  // Remove this function call once you have "real" code.
  clearLCDLine(0);                      // Clear line 1 (0) of the LCD
  clearLCDLine(1);                      // Clear line 2 (1) of the LCD
  bLCDBacklight = true;                 // Turn on LCD Backlight
	clearTimer(T1);
		if (score_mode==0) return;
	timeDrop=time1[T1];
	return;
}


int minTurn = 20;
int state6U = 0;
int state6D =0;


/*---------111111111111111------------------------------------------------------------------*/
/*                                                                           */
/*                              User Control Task                            */
/*                                                                           */
/*  This task is used to control your robot during the user control phase of */
/*  a VEX Competition.                                                       */
/*                                                                           */
/*  You must modify the code to add your own robot specific commands here.   */
/*---------------------------------------------------------------------------*/

task usercontrol()
{
  // User control code here, inside the loop
  clearLCDLine(0);                  	// Clear line 1 (0) of the LCD
  clearLCDLine(1);                  	// Clear line 2 (1) of the LCD
  bLCDBacklight = true;             	// Turn on LCD Backlight
	clearTimer(T1);

	while(true)
	{
		int verticalPower = vexRT[Ch3]; // get joystick value for right vertical channel
		int horizontalPower = vexRT[Ch4]; // get joystick value for left vertical channel

		move2D(verticalPower, horizontalPower);

		if(vexRT[Btn6U]){
			if(state6U % 2 == 0){
					motor[intakeMotor] = 70;
			}
			if(state6U % 2 == 1){
		  		motor[intakeMotor] = 0;
		  		motor[rollerMotor] = 0;
			}

			wait1Msec(500);
			state6U += 1;
		}


		if(vexRT[Btn6D]) {
			if(state6D % 2 == 0){
					motor[rollerMotor] = 70;
			}
			if(state6D % 2 == 1){
		  		motor[rollerMotor] = -70;
			}

			wait1Msec(500);
			state6D += 1;
		}


}
}

	void move2D(int motorPower, int turnPower){
		    if (abs(turnPower) > minTurn) {
        turnPower -= abs(turnPower)/turnPower *minTurn;
    }
    else turnPower = 0;

    int leftPower = motorPower + turnPower;
    int rightPower = motorPower - turnPower;
    if (abs(leftPower)> 100){
        leftPower = 100*(leftPower/abs(leftPower));
    }
    if (abs(rightPower) > 100) {
        rightPower = 100*(rightPower/abs(rightPower));
    }

    motor[rightFrontMotor] = rightPower;
    motor[rightBackMotor] = rightPower;
    motor[leftFrontMotor] = leftPower;
    motor[leftBackMotor] = leftPower;

	}
